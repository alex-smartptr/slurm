AR      := ar
CC      := gcc
LD      := ld
OBJCOPY := objcopy
RM      := rm
MV      := mv
TOUCH   := touch
MKDIR   := mkdir

INSTALL := install -c
INSTALL_DATA = $(INSTALL) -m 644
INSTALL_PROGRAM = $(INSTALL)
